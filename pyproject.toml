[tool.poetry]
name = "markov-bot"
version = "0.1.0"
description = ""
authors = ["SSS_Krut"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
chardet = "^5.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
